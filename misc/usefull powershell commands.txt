
Below, in more detail are Windows Command Prompt commands that will allow you to:
- provide for Over The Ethernet updates,
- tests for NTP responses,
- reset the Meinberg drift file (to recreate a new baseline for the use of a different time server)

Notes: In the examples below:

   the device name, ESP32TimeServer, is set as DeviceName in ESP32TimeServerSettings.h
   xx.xx.xx.xx is the ip address of the esp32-p4 running the esp32 time server software

1. Here are a couple example command line commands which may be used, following a build, to provide for an Over The Ethernet (OTE) update:
   (the path name which precedes ESP32TimeServer.bin may vary)
      
   python tools\espota.py -i ESP32TimeServer.local -p 3232 -P 3232 -a ESP32TimeServerpw -f build-esp32p4-rev-1-3\ESP32TimeServer.bin -r -d
   or
   python tools\espota.py -i x.x.x.x -p 3232 -P 3232 -a ESP32TimeServerpw -f build-esp32p4-rev-1-3\ESP32TimeServer.bin -r -d


2. Here is a command which may be used for sending a NTP test request:
   
   w32tm /stripchart /computer:ESP32TimeServer.local /samples:5 /dataonly
   or
   w32tm /stripchart /computer:xx.xx.xx.xx /samples:5 /dataonly  

   With the output showing, for example:

   Tracking 192.168.7.24 [192.168.7.24:123].
   Collecting 5 samples.
   The current time is 2026-08-24 8:23:44 AM.
   08:23:44, +00.0022650s
   08:23:46, +00.0016820s
   08:23:48, +00.0016135s
   08:23:50, +00.0017035s
   08:23:52, +00.0018610s

   The first number above is the current time.
   The second number above is the clock offset between the machine issuing the command and the esp32timeserver.local devices ESP32TimeServer
   (in the case above approximately 4/10000's of a second).

3. Here is an alternative command which may be used for sending a NTP test request:

   ntpdate -q 192.168.7.25

   With the output showing, for example:
   24 Aug 08:20:01 ntpdate[48144]: Raised to high priority class, realtime requires Increase Scheduling Priority privilege (enabled with secpol.msc).
   server 192.168.7.25, stratum 1, offset +0.003054, delay 0.04068
   24 Aug 08:20:01 ntpdate[48144]: adjust time server 192.168.7.25 offset +0.003054 sec


4. If you are using the Meinberg NTP Time Server https://www.meinbergglobal.com/english/sw/ntp.htm
   and you want to create a new baseline as you are now using a new time server, 
   then from the Windows Command Prompt with Administrative Privileges you can use these commands:
   
   net stop ntp
   del "C:\Program Files (x86)\NTP\etc\ntp.drift"
   net start ntp 
   
   (between 15 minutes to a hour later a new ntp.drift file will be created by the service)

4. Here is an other open source project (also written by me) that allows you to stress test your NTP server via a Window approximately
   

   